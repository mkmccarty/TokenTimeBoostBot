name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: freebsd-latest

    steps:
    - uses: actions/checkout@v3

    - name: build application
      run: make freebsd
